# S10：从 200 轮续训至累计 1000 轮

## 执行记录

本报告随本次唯一续训更新。最终次数与评估以文末结果和机器可读证据为准。

- 基准提交：`55838bb959e64d50ab787fc861fcd9f1c8ce7465`。
- 本任务独立 worktree：`C:\Users\Lenovo\.codex\worktrees\a772\HIMLoco-for-Go2W`。
- 独立服务器源码：`/data/HIMLoco-S10-resume-20260915`。
- 独立运行目录：`/data/HIMLoco-S10-resume-20260915/logs/S10_HIM/resume_200_to1000_20260915`。
- 镜像：复用 `him-gym:py38-torch201-cu118`；Isaac Gym 与扩展缓存沿用已有挂载。
- 预算：恢复累计 200，新增最多 800；累计 500 检查，累计 1000 正常结束。4096 环境 × 48 步/轮，每 50 轮保存。
- 本轮没有子代理、A/B、参数扫描、停车奖励/采样改动、清零策略动作、锁轮或真机部署。

## 配方、恢复与日志

完整沿用已经训练 200 轮的候选：PD 2.5 ms、decimation=8，策略/历史/奖励周期 20 ms，延迟为 0/5/10/15 ms。腿 Kp/Kd=80/2，轮 0/0.6；hipx/hipy/knee/轮动作缩放分别为 .125/.25/.25/5。资产、限矩、奖励、PPO/HIM、噪声、域随机化、混合地形均未改变。

通过 `HIMOnPolicyRunner.load(..., load_optimizer=True)` 恢复 actor、critic、HIM encoder/target/prototypes、已学到的 16D std、两套 Adam 及学习率。零 PPO 检查确认全部模型及优化器张量与源 checkpoint 逐元素相等，累计迭代 200、历史环境步 39,321,600、两套 Adam 各 4000 步，两套学习率及相应算法属性均为 1e-5。未重置探索 std 或学习率。

源 checkpoint 没有模拟器、随机数或课程状态。本次按原 seed/config 重新生成环境、地形和域随机化，初始地形平均等级 2.512695（原 200 轮末约 1.54），命令范围 vx ±1、vy ±.6、yaw ±1；首次开始时按原流程随机化回合长度。因此这不是逐物理步无缝恢复，开头回报/回合统计含新回合启动效应。500 轮检查在同一个 `learn(800)` 调用中保留 runner、模拟器和课程；checkpoint 的 infos 另存地形等级、类型、原点、命令范围与课程步数。

唯一算法文件改动为日志：HIM 返回已计算的全轮小批次平均损失，保持四返回值及原梯度计算；从已有 KL/ratio 记录每轮 20 个小批次 KL、KL 均值、最大小批次均值、clip fraction、各小批次学习率。没有额外采样或更改优化顺序/自适应规则。201 轮后的 HIM 平均损失与旧 1–200 轮的最后小批次损失不可直接当作同口径序列。

## 启动前检查异常（新增 PPO = 0）

首次启动在保存恢复状态时，有限值检查将 CPU 上的 Adam step 与 GPU 状态的布尔张量堆叠，触发设备不一致异常。尚未采样、尚未更新，两套优化器仍各 4000 步。保留失败 checkpoint、模拟器状态、恢复核验、日志和源码：服务器 `logs/S10_HIM/preflight_failed_20260915/`、`preflight-failure.log`、`source-preflight.tar.gz`。

修正仅使检查用的布尔张量处于同一设备，并新增 CPU/CUDA 混合状态、NaN/Inf 拒绝和递归状态比较自检；通过后重新完成零 PPO 恢复核验，再进入本次 800 轮续训。没有调参或重跑已发生的训练。实际训练源码冻结为 `source-training.tar.gz`；评估/绘图脚本另存 `source-evaluation.tar.gz`。

可运行检查：`check_s10_candidate.py --self-check`、`check_s10_update_stats.py`、`resume_s10_candidate.py --self-check`、`stand_s10_candidate.py --self-check`。检查包括候选延迟/标准差兼容性、实际 HIMPPO.update 的平均损失与统计、混合设备有限值检查、首步接触门槛与短回放命令时段。

## 500 轮中途检查

### 运行中更新的 Git 协作规则

约累计 250 轮时收到用户的新规则：本地提交推送，服务器 fetch 后固定完整 SHA，再启动训练；训练中的同一目录不得更新源码。当前一次 `learn(800)` 已在规则更新前启动，来源仍如上归档，不能追溯改写为 Git 启动。后续不修改该运行目录；本地用 `JackNcodex/s10-resume-1000-20260915` 分支发布必要代码，在服务器另一独立 Git 检出固定提交供后续进程使用。500 后若检查通过，原存活 runner 继续该调用，没有新训练启动或切换其源码。后续任何新训练必须走 Git 并在日志记录完整 SHA。

Git 发布版本将输入 checkpoint 放在忽略的 `artifacts/s10-resume-input/`，将原训练配置作为代码输入纳入 `legged_gym/scripts/configs/s10_resume_200.json`，启动时要求干净 Git 检出并记录 `source_commit.txt`。这部分追溯措施只适用于未来新启动，不改变已经运行的进程。

待本次训练达到累计 500 后填入。

## 最终训练与评估

待预算正常结束或实际训练异常停止后填入。

## 证据入口

- [恢复核验](evidence/s10-resume-20260915/resume_verification.json)
- [原候选配置](evidence/s10-resume-20260915/source_config.json)
- [原候选逐轮指标](evidence/s10-resume-20260915/source_metrics.jsonl)
- 本地冻结源码与产物：`artifacts/s10-resume-20260915/`。
- 站姿沿用修正后的创建初态，记录首个 PD 子步，保留全部接触帧，无预热和中途重置。
- 短回放仅为 10 秒平地基本响应：2 秒零指令、4 秒 vx=.5、2 秒 yaw=.4、2 秒零指令；不扩展为专项运动能力验收。
- 视频使用保存的 Gym/MuJoCo 位姿回放。MuJoCo 物理由服务器 3.2.3 计算，渲染采用本地 3.3.7，不重算物理。
